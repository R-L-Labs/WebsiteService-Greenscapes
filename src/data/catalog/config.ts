// Catalog display settings

/**
 * Show estimated prices on catalog cards.
 * Prices are already in the data files — flip this to true to display them.
 * While false, cards show a "Free Estimate" link instead.
 */
export const showPrices = false;
